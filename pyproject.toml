[project]
name = "my-makesite"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown",
    "htmlgenerator",
    "htbuilder",
    "pyyaml>=6.0.2",
]
